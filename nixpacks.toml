[variables]
NIXPACKS_NODE_VERSION = "22"

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["node -e \"console.log('Skipping web build for Railway worker service')\""]

[start]
cmd = "npm run worker:start"
